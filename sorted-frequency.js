function sortedFrequency(arr, num) {
  let numArr = arr;

  while (numArr[0] != num) {
    const arrLength = numArr.length - 1;
    const median = Math.floor(arrLength / 2);

    if (numArr[median] == num) {
    } else {
    }
  }
}

module.exports = sortedFrequency;
